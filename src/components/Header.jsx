// Importing Nav and css
import '../index.css'
import Nav from './Nav'

// For now, Header only has the nav menu in it
export default function Header () {
    return (
        <>
            <Nav />
        </>
    ) 
}